# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/skillsync

# JWT Secret Key (Generate a strong secret key for production)
JWT_SECRET=your_jwt_secret_key_here

# Server Port
PORT=5000

# Node Environment
NODE_ENV=production

# Client URL for CORS (update for production deployment)
CLIENT_URL=https://skill-sync-lyart.vercel.app

